#include "../header/codexion.h"

void    *verif_coder(t_coder dje)
{
    printf("", dje.id);
    return (NULL);
}


int main(int argc, char **argv)
{
    t_coder thread_1;
    t_coder thread_2;
    t_coder thread_3;
    int i = 0
    
    tab = [thread_1, thread_2, thread_3];
    while(tab[i])
    {
        pthread_create(&tab[i], NULL, verif_coder, )
    } 
}